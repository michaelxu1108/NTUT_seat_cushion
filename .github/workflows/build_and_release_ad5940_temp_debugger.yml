name: 🛠 Build & Release AD5940 Temporary Debugger

on:
  push:
    tags:
      - 'apps/ad5940_temp_debugger/v*'
  workflow_dispatch:

permissions:
  contents: write   # allow creating/updating Releases & uploading assets
  actions:  read    # allow pulling marketplace actions

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build_and_release_android:
    name: Build and Release Android
    runs-on: ubuntu-latest

    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v3

      - name: 🚀 Cache pub deps
        uses: actions/cache@v3
        with:
          path: ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.yaml') }}
          restore-keys: ${{ runner.os }}-pub-

      - name: ☕️ Setup Java (Temurin 17)
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: '17'

      - name: 🦋 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: 📥 Get dependencies
        run: |
          cd apps/ad5940_temp_debugger
          flutter pub get

      - name: ✅ Run tests
        run: |
          cd apps/ad5940_temp_debugger
          flutter test --coverage

      - name: 🏗️ Build APKs
        run: |
          cd apps/ad5940_temp_debugger
          flutter build apk --release --split-per-abi

      - name: 📦 Prepare artifacts
        run: |
          mkdir -p artifacts
          mv apps/ad5940_temp_debugger/build/app/outputs/flutter-apk/*.apk artifacts
          cd artifacts
          for f in *; do mv "$f" "ad5940_temp_debugger_$f"; done

      - name: 🏷️ Create or update Release
        uses: ncipollo/release-action@v1.16.0
        with:
          tag:       ${{ github.ref_name }}
          name:      Release ${{ github.ref_name }}
          artifacts: artifacts/*
          token:     ${{ secrets.GITHUB_TOKEN }}
          allowUpdates: true
          omitBody: true
          omitBodyDuringUpdate: true
          omitNameDuringUpdate: true
          omitPrereleaseDuringUpdate: true
          replacesArtifacts: true
          removeArtifacts: false
